[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests_homeassistant"]

[tool.coverage.run]
branch = true
source = ["custom_components.cez_dynamic_tariff"]

[tool.coverage.report]
show_missing = true
skip_covered = true
